<?php
if ($this->form)
{
	$form=$this->form;
	$form->setAttribute('action', $this->url('order/default', array('controller'=>'cost','action'=>'update')));
	$form->prepare();
	echo $this->form()->openTag($form);
	echo $this->formCollection($form);
	echo $this->form()->closeTag();
}